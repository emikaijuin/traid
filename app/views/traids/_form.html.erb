<%= form_for(@traid) do |f| %>
  <% if @traid.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@traid.errors.count, "error") %> prohibited this traid from being saved:</h2>

      <ul>
      <% @traid.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
